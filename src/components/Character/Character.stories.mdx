import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import Character from '.'

<Meta title="Character" component={Character} />

# Character

<Canvas>
  <Story name="Basic" args={{ id: '1' }}>
    {args => <Character {...args} />}
  </Story>
</Canvas>

Test component to show Apollo Client usage. Fetches character data from **Rick & Morty API** by provided `id`.

<ArgsTable story="." />

## Wrong id

Component renders fallback data if error occurs / data is empty / in loading process.

<Canvas>
  <Story name="Wrong id" args={{ id: '1000' }}>
    {args => <Character {...args} />}
  </Story>
</Canvas>
