import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks'
import SelectCharacter from '.'

<Meta title="SelectCharacter" component={SelectCharacter} />

# SelectCharacter

<Canvas>
  <Story name="Basic">{args => <SelectCharacter {...args} />}</Story>
</Canvas>

Test component to show Apollo Client usage. Fetches characters count from **Rick & Morty API** and render button to pass random id to `Character` component.

<Props story="Basic" />
