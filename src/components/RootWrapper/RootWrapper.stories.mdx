import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import RootWrapper from '.'

<Meta title="RootWrapper" component={RootWrapper} />

# RootWrapper

<Canvas>
  <Story name="Basic" args={{ children: 'Site content' }}>
    {args => <RootWrapper {...args} />}
  </Story>
</Canvas>

Wrapper with all providers to add in Gatsby `wrapRootElement`, `@test-utils` and `Storybook` global decorator.

<ArgsTable />
