import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import StaticImage from '.'

<Meta title="StaticImage" component={StaticImage} />

# StaticImage

<Canvas>
  <Story name="Basic">{args => <StaticImage {...args} />}</Story>
</Canvas>

Test component to show `useStaticQuery` usage. Fetches fluid image with `gatsby-image`.

<ArgsTable story="." />
