import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks'
import Seo from '.'

<Meta title="Seo" component={Seo} />

# Seo

```
<Canvas>
  <Story name="Basic">{args => <Seo {...args} />}</Story>
</Canvas>
```

Adds seo tags in head via `react-helmet`. By default uses fields from project config, but you can customize main fields with props.

```
<Props story="Basic" />
```
