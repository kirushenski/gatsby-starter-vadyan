import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks'
import GlobalStyles from '.'

<Meta title="GlobalStyles" component={GlobalStyles} />

# GlobalStyles

<Canvas>
  <Story name="Basic">{args => <GlobalStyles {...args} />}</Story>
</Canvas>

Global styles for site. Uses `Global` module from `Emotion`. Includes `focus-visible`, `normalize`, `@font-face` declarations and basic tags styles. Component is included in `RootWrapper`.

<Props story="Basic" />
